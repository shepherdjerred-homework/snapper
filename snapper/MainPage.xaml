<Page
    x:Class="snapper.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:snapper"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="4*" />
        </Grid.ColumnDefinitions>
        <NavigationView Background="{ThemeResource SystemControlAcrylicWindowBrush}" Height="Auto" Width="Auto"
                        Grid.Row="0" Grid.Column="0">
        </NavigationView>
        <ListView Height="Auto" Width="Auto" Grid.Row="0" Grid.Column="1" />
        <StackPanel Grid.Row="0" Grid.Column="2">
            <RichEditBox Height="Auto" Width="Auto" />
        </StackPanel>
    </Grid>
</Page>